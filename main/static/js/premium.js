function showProductDetails(product) {
    alert(`Details for ${product}`);
}

function showCard(cardIndex) {
    // Logic to display industry details based on the card clicked
    alert(`Details for card ${cardIndex}`);
}
